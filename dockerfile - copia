# Usa una imagen base de Node.js 20
FROM node:20-slim

# Directorio de trabajo en el contenedor
WORKDIR /usr/src/app

# Copia los archivos de configuración de Node.js y realiza la instalación de dependencias
COPY package*.json ./
RUN npm install

# Copia el resto de los archivos del proyecto al contenedor
COPY . .

# Si tienes dependencias Python, puedes instalar Python y sus dependencias aquí
# Descomenta y ajusta la sección a continuación si es necesario:
# RUN apt-get update && \
#     apt-get install -y python3 python3-pip && \
#     pip install --no-cache-dir -r requirements.txt

# Expón el puerto si es necesario (opcional)
# EXPOSE 3000

# Define el comando por defecto para ejecutar tu aplicación
CMD [ "node", "index.js" ]
